# Security & Compliance (Draft)

Document how the Barcelona deployment protects participant data.

## Topics to capture

- Encryption strategy (on-device RSA, server-side storage, key rotation schedule).
- Credential management (API tokens, SSH keys, `.env` handling, per-environment secrets).
- Data retention policy (how long surveys/locations are stored, deletion requests, GDPR obligations).
- Incident response checklist (detection, communication, containment).
- Third-party audits or reviews.

Once the DigitalOcean stack is online, update this file with real commands and links to monitoring dashboards.
