name: CI_tests
on: [push, pull_request]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    # Setup FVM and Flutter
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.27.1'
        channel: 'stable'
        cache: true
        cache-key: 'flutter-3.27.1'
        
    - name: Cache Flutter dependencies
      uses: actions/cache@v4
      with:
        path: |
          .dart_tool
          build
          /home/runner/.pub-cache
        key: ${{ runner.os }}-flutter-deps-${{ hashFiles('pubspec.lock') }}
        restore-keys: |
          ${{ runner.os }}-flutter-deps-
         
    - name: Get flutter dependencies
      run: |
        flutter pub get
    
    - name: Statically analyze the Dart code for any errors.
      run: |
        timeout 300 flutter analyze --no-fatal-infos --no-fatal-warnings || echo "Analysis completed with warnings"
    
    - name: Run tests for our flutter project.
      run: |
        export FLUTTER_TEST_MODE=true
        export CI=true
        timeout 600 flutter test --coverage --dart-define=FLUTTER_TEST_MODE=true --dart-define=CI=true --reporter=expanded --concurrency=1 --timeout=60s --exclude-tags=platform --exclude-tags=integration --suppress-analytics
      continue-on-error: false
        
    - name: Check coverage file exists
      run: |
        ls -la coverage/ || echo "Coverage directory not found"
        
    - name: Upload coverage to codecov
      if: always()
      uses: codecov/codecov-action@v5
      with:
        files: ./coverage/lcov.info
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: false
        verbose: false

    - name: Upload test results to Codecov
      if: ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}

    - name: Detect unintended Android package changes
      run: |
        ./check-version.sh