name: Deploy Production Release to GitHub Releases

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+' # Only trigger on production tags like v1.0.0 (no beta suffix)
      - 'v[0-9]+.[0-9]+.[0-9]+-[0-9]+' # Also support tags with build numbers like v1.0.0-123

permissions:
  contents: write
  packages: write

jobs:  
  build:
    runs-on: macos-latest # Use macOS for iOS builds
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        flutter-version: '3.27.1' # Match your fvm version
    
    - name: Get version from tag
      id: version
      run: echo "content=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

    - name: Check if this is a production release
      id: release_type
      run: |
        TAG="${GITHUB_REF#refs/tags/}"
        echo "Processing tag: $TAG"
        
        if [[ "$TAG" =~ -beta ]]; then
          echo "ERROR: Beta tag detected in production workflow!"
          echo "Tag '$TAG' contains '-beta' suffix which should not trigger this workflow."
          echo "Beta tags should only trigger the 'CD-deploy-beta-releases.yml' workflow."
          echo "If you see this error, the workflow trigger patterns need to be updated."
          exit 1
        fi
        
        # Verify tag matches expected production pattern
        if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9]+)?$ ]]; then
          echo "ERROR: Invalid production tag format!"
          echo "Expected format: v1.0.0 or v1.0.0-123 (semantic version with optional build number)"
          echo "Received: $TAG"
          exit 1
        fi
        
        echo "Confirmed valid production release tag: $TAG"
        echo "is_production=true" >> $GITHUB_OUTPUT

    - name: Extract and verify version information
      working-directory: gauteng-wellbeing-mapper-app
      run: |
          # Get version from pubspec.yaml (remove build number)
          PUBSPEC_VERSION=$(grep "version:" pubspec.yaml | sed 's/version: //' | sed 's/+.*//' | tr -d ' ')
          
          # Get version from git tag (remove 'v' prefix)
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          
          echo "pubspec.yaml version: $PUBSPEC_VERSION"
          echo "Git tag version: $TAG_VERSION"
          
          if [ "$PUBSPEC_VERSION" = "$TAG_VERSION" ]; then
            echo "Versions match!"
          else
            echo "Version mismatch between pubspec.yaml ($PUBSPEC_VERSION) and git tag ($TAG_VERSION)"
            echo "Please update pubspec.yaml version to match the git tag"
            exit 1
          fi

    # CRITICAL: Clean all caches before building
    - name: Clean all build caches
      working-directory: gauteng-wellbeing-mapper-app
      run: |
        echo "🧹 Cleaning all build caches to ensure correct version..."
        
        # Clean Flutter build cache
        flutter clean
        
        # Clean Android gradle cache (recommended for AAR metadata issues)
        cd android && ./gradlew clean && cd ..
        
        # Remove iOS build artifacts
        rm -rf ios/Pods
        rm -rf ios/.symlinks
        rm -rf ios/Flutter/Flutter.framework
        rm -rf ios/Flutter/Flutter.podspec
        
        # Remove Android build artifacts and caches
        rm -rf android/.gradle
        rm -rf android/app/build
        rm -rf android/build
        rm -rf .dart_tool
        rm -rf build
        
        # Remove any existing local.properties
        rm -f android/local.properties
        
        echo "✅ All caches cleaned"

    - name: Get flutter dependencies
      run: flutter pub get
      working-directory: gauteng-wellbeing-mapper-app

    # Fix AAR metadata compatibility issues
    - name: Initialize Android environment
      working-directory: gauteng-wellbeing-mapper-app
      run: |
        echo "🔧 Initializing Android environment for AAR compatibility..."
        
        # Ensure Flutter has initialized the Android project properly
        flutter precache --android
        
        # Check that gradlew is available and executable
        if [ -f "android/gradlew" ]; then
          chmod +x android/gradlew
          echo "✅ Gradle wrapper is ready"
        else
          echo "⚠️ Gradle wrapper not found - Flutter will create it during build"
        fi
        
        echo "✅ Android environment initialized"

    - name: Sync version information
      run: |
        echo "🔄 Syncing version information using sync-version.sh..."
        chmod +x sync-version.sh
        ./sync-version.sh
        echo "✅ Version sync complete"

    - name: Verify iOS entitlements configuration
      run: |
        echo "Verifying iOS entitlements are properly configured..."
        
        # Check entitlements file exists
        if [ ! -f "ios/Runner/Runner.entitlements" ]; then
          echo "ERROR: Entitlements file missing!"
          exit 1
        fi
        
        # Check entitlements are linked in Xcode project
        if ! grep -q "CODE_SIGN_ENTITLEMENTS = Runner/Runner.entitlements" ios/Runner.xcodeproj/project.pbxproj; then
          echo "ERROR: Entitlements not linked in Xcode project!"
          exit 1
        fi
        
        # Count linked configurations
        ENTITLEMENT_COUNT=$(grep -c "CODE_SIGN_ENTITLEMENTS = Runner/Runner.entitlements" ios/Runner.xcodeproj/project.pbxproj)
        if [ "$ENTITLEMENT_COUNT" -lt 3 ]; then
          echo "ERROR: Entitlements not linked in all build configurations!"
          exit 1
        fi
        
        # Check location permissions in Info.plist
        REQUIRED_KEYS=("NSLocationAlwaysAndWhenInUseUsageDescription" "NSLocationAlwaysUsageDescription" "NSLocationWhenInUseUsageDescription" "NSLocationUsageDescription")
        for key in "${REQUIRED_KEYS[@]}"; do
          if ! grep -q "$key" ios/Runner/Info.plist; then
            echo "ERROR: Missing location permission key: $key"
            exit 1
          fi
        done
        
        echo "iOS entitlements configuration verified"
      working-directory: gauteng-wellbeing-mapper-app

    - name: Statically analyze the Dart code for any errors
      run: flutter analyze
      working-directory: gauteng-wellbeing-mapper-app
    
    - name: Run tests for our flutter project
      run: flutter test --dart-define=FLUTTER_TEST_MODE=true
      working-directory: gauteng-wellbeing-mapper-app

    - name: Generate and verify local.properties with version info
      run: |
        echo "📝 Creating local.properties with correct version information..."
        
        # Extract version and build number from pubspec.yaml
        FULL_VERSION=$(grep "version:" pubspec.yaml | sed 's/version: //' | tr -d ' ')
        VERSION_NAME=$(echo $FULL_VERSION | cut -d'+' -f1)
        VERSION_CODE=$(echo $FULL_VERSION | cut -d'+' -f2)
        
        echo "Extracted from pubspec.yaml:"
        echo "  Full version: $FULL_VERSION"
        echo "  Version name: $VERSION_NAME" 
        echo "  Version code: $VERSION_CODE"
        
        # Create local.properties with correct version info
        cat > android/local.properties << EOF
        sdk.dir=/opt/android-sdk-linux
        flutter.sdk=/opt/hostedtoolcache/flutter
        flutter.buildMode=release
        flutter.versionName=$VERSION_NAME
        flutter.versionCode=$VERSION_CODE
        EOF
        
        echo "Generated local.properties:"
        cat android/local.properties
        
        # Also update gradle.properties to ensure version is used
        echo "" >> android/gradle.properties
        echo "# Version overrides from CI" >> android/gradle.properties
        echo "versionName=$VERSION_NAME" >> android/gradle.properties
        echo "versionCode=$VERSION_CODE" >> android/gradle.properties
        
        echo "Updated gradle.properties:"
        tail -5 android/gradle.properties
      working-directory: gauteng-wellbeing-mapper-app

    # Android Builds - Production Flavor
    - name: Build Android APKs (Production) - Multiple Architectures
      run: |
        echo "🚀 Building Production Android APKs for multiple architectures..."
        
        # Extract version info for command-line override
        FULL_VERSION=$(grep "version:" pubspec.yaml | sed 's/version: //' | tr -d ' ')
        VERSION_NAME=$(echo $FULL_VERSION | cut -d'+' -f1)
        VERSION_CODE=$(echo $FULL_VERSION | cut -d'+' -f2)
        
        echo "Using version: $VERSION_NAME (code: $VERSION_CODE)"
        
        # Ensure Android environment is ready
        echo "🔍 Checking Android environment..."
        flutter doctor -v
        
        # Build APKs with simplified parameters first
        echo "🏗️ Starting APK build..."
        flutter build apk --release \
          --flavor=production \
          --build-name="$VERSION_NAME" \
          --build-number="$VERSION_CODE" \
          --split-per-abi \
          --verbose || {
            echo "❌ Build failed. Checking for common issues..."
            
            # Try without split-per-abi as fallback
            echo "🔄 Retrying without split-per-abi..."
            flutter build apk --release \
              --flavor=production \
              --build-name="$VERSION_NAME" \
              --build-number="$VERSION_CODE" \
              --verbose || {
              echo "❌ Build failed completely. Dumping debug info..."
              echo "📋 Flutter version:"
              flutter --version
              echo "📋 Gradle wrapper status:"
              ls -la android/gradlew || echo "No gradlew found"
              echo "📋 Android directory contents:"
              ls -la android/
              exit 1
            }
          }
        
        echo "✅ Production Android APKs built successfully!"
        echo "APK locations:"
        find build/app/outputs/flutter-apk/ -name "*.apk" -type f | sort
        
        # Verify APK version using aapt (more reliable than strings)
        echo "🔍 Verifying APK versions with aapt..."
        for apk in build/app/outputs/flutter-apk/*.apk; do
          if [ -f "$apk" ]; then
            echo "Checking: $(basename "$apk")"
            # Try to use aapt if available
            if command -v aapt &> /dev/null; then
              aapt dump badging "$apk" | grep -E "package:|versionCode|versionName" || true
            else
              # Fallback to checking AndroidManifest.xml content
              unzip -p "$apk" AndroidManifest.xml | strings | grep -E "1\.[0-9]+\.[0-9]+" | head -5 || true
            fi
          fi
        done
      working-directory: gauteng-wellbeing-mapper-app

    - name: Build Android App Bundle (Production)
      run: |
        echo "📦 Building Production Android App Bundle..."
        
        # Extract version info for command-line override
        FULL_VERSION=$(grep "version:" pubspec.yaml | sed 's/version: //' | tr -d ' ')
        VERSION_NAME=$(echo $FULL_VERSION | cut -d'+' -f1)
        VERSION_CODE=$(echo $FULL_VERSION | cut -d'+' -f2)
        
        flutter build appbundle --release \
          --flavor=production \
          --dart-define=APP_FLAVOR=production \
          --build-name="$VERSION_NAME" \
          --build-number="$VERSION_CODE"
        
        echo "✅ Production App Bundle built successfully!"
        echo "AAB location:"
        ls -la build/app/outputs/bundle/productionRelease/app-production-release.aab || echo "AAB file not found"
      working-directory: gauteng-wellbeing-mapper-app

    # iOS Builds - Production Flavor
    - name: Install iOS dependencies
      run: |
        cd ios 
        pod cache clean --all
        pod install --repo-update
        cd ..
      working-directory: gauteng-wellbeing-mapper-app
      
    - name: Build iOS (Production) - Unsigned for Distribution
      run: |
        echo "🍎 Building iOS Production without code signing for distribution..."
        
        # Set the appropriate Info.plist for production flavor
        cp ios/Runner/Info-Production.plist ios/Runner/Info.plist
        echo "Using Production Info.plist with bundle ID: com.github.activityspacelab.wellbeingmapper.gauteng"
        
        # Build iOS without code signing (for distribution only)
        flutter build ios --release \
          --dart-define=APP_FLAVOR=production \
          --no-codesign
        
        echo "✅ iOS Production build complete (unsigned)!"
        echo "iOS build location:"
        ls -la build/ios/iphoneos/ || echo "iOS build directory not found"
        
        # Create a zip archive of the iOS app for distribution
        cd build/ios/iphoneos/
        zip -r ../../../wellbeing-mapper-production-ios.zip Runner.app/
        cd ../../../
        
        echo "iOS app archived as wellbeing-mapper-production-ios.zip"
        echo "Note: This build is for distribution - actual App Store submission requires proper provisioning profiles and signing"
      working-directory: gauteng-wellbeing-mapper-app
    
    - name: Create a Production Release in GitHub
      uses: ncipollo/release-action@v1
      with:
        artifacts: |
          gauteng-wellbeing-mapper-app/build/app/outputs/flutter-apk/*.apk
          gauteng-wellbeing-mapper-app/build/app/outputs/bundle/productionRelease/app-production-release.aab
          gauteng-wellbeing-mapper-app/wellbeing-mapper-production-ios.zip
        token: ${{ secrets.GITHUB_TOKEN }}
        tag: ${{ steps.version.outputs.content }}
        name: "Wellbeing Mapper ${{ steps.version.outputs.content }} - Production Release"
        body: |
          # Wellbeing Mapper Production Release ${{ steps.version.outputs.content }}
          
          **This is the official production release of Wellbeing Mapper!**
          
          ## What is Wellbeing Mapper?
          Wellbeing Mapper helps you track your mental wellbeing and movement patterns. This production version offers both private personal use and research participation options.
          
          ## What's New in This Release
          - Stable production build with full testing
          - Enhanced privacy controls and data protection
          - Improved user interface and experience
          - Comprehensive location and survey features
          - Research participation capabilities (optional)
          
          ## How to Download and Install
          
          ### Android Users
          **Download one of the production APK files below:**
          - Choose any `app-production-*.apk` file (Flutter automatically optimizes for your device architecture)
          - All APK variants work on modern Android devices - download any one
          
          **Installation steps:**
          1. Download any APK file from the Assets section below
          2. Enable "Install unknown apps" in your device's security settings
          3. Tap the APK file and follow the installation prompts
          4. Open Wellbeing Mapper and choose your preferred mode
          
          ### Android App Bundle
          The `app-production-release.aab` file is for Google Play Store distribution.
          
          ### iOS Users
          **iOS builds are included but require proper signing for installation:**
          - Download `wellbeing-mapper-production-ios.zip` which contains the unsigned iOS app
          - iOS builds in this release are unsigned (for distribution purposes)
          - For device installation, contact the research team for TestFlight access
          - App Store distribution requires additional signing and review process
          - Developers can re-sign the build with their own provisioning profiles
          
          ## Privacy & Data Protection
          - **Private Mode**: All data stays securely on your device
          - **Research Mode**: Optional data sharing with explicit consent
          - **No Tracking**: No advertising or third-party analytics
          - **Your Control**: Delete all data anytime from app settings
          
          ## Production vs Beta
          This is the **stable production release** - recommended for:
          - Regular users wanting reliable functionality
          - Research participants in official studies
          - Long-term personal wellbeing tracking
          
          For testing new features, see our beta releases with `-beta` tags.
          
          ## Support & Feedback
          - Email: john.palmer@upf.edu
          - Full Documentation: [User Guide](https://github.com/ActivitySpaceLab/gauteng-wellbeing-mapper-app/blob/main/docs/USER_GUIDE.md)
          - Report Issues: Use GitHub Issues or in-app feedback
          
          ## What's Included
          - Multiple Production Android APKs (automatically optimized for different devices)
          - Android App Bundle for Play Store
          - iOS app archive ready for App Store submission or re-signing
          - Full test coverage and quality assurance
          - Complete privacy and security implementations
        draft: false
        prerelease: false